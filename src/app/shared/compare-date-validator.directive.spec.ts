import { CompareDateValidatorDirective } from './compare-date-validator.directive';

describe('CompareDateValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CompareDateValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
